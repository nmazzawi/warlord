{
  "name": "warlord",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Warlord — a browser-first 2D conquest action-RPG (Milestone 1: the raid loop)",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "smoke": "node tests/smoke.mjs"
  },
  "dependencies": {
    "@fontsource/cinzel": "^5.3.0",
    "@fontsource/nunito-sans": "^5.3.0",
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "5.9.3",
    "vite": "7.3.6"
  }
}
